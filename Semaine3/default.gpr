project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("test_opapplication.adb");

   package Compiler is
      for Switches ("ada") use ("-g", "-O2");
   end Compiler;

end Default;

